<div class="container pt-5 pb-5">
  <div class="row mt-5 mb-5">
    <div class="col-lg-10">
      <h2>ログイン</h2>

      <p>会員の方はこちらからログイン</p>

        <%= form_with model: @customer, url: customer_session_path, local: true do |f| %>

          <div class="form-group">
            <%= f.label "メールアドレス", class: "col-3 mt-2" %>
            <%= f.email_field :email, autofocus: true, placeholder: "sample@example.com", class: "col-7", autocomplete: "email" %>
          </div>

          <div class="form-group">
            <%= f.label "パスワード", class: "col-3 mt-2" %>
            <%= f.password_field :password, autocomplete: "current-password", placeholder: "パスワード", class: "col-7" %>
          </div>

          <div class="actions text-center">
             <%= f.submit "ログイン", class: "btn btn-primary col-3 mt-2" %>
          </div>

        <% end %>
        </br></br>

        <h3 class="mb-3">会員登録がお済みでない方</h3>
          <%= link_to "こちら", new_customer_registration_path %>　から新規登録を行ってください。

    </div>
  </div>
</div>